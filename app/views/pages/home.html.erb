<!DOCTYPE html>
<html>
    <head>
        <title>CodeSandbox Starter</title>
        <meta name="viewport" content="width=device-width,initial-scale=1">
        <style>
        .home-container {
            width: 100%;
            display: flex;
            overflow: auto;
            align-items: center;
            flex-direction: column;
        }
        .home-text {
            font-size: 3rem;
            max-width: 600px;
            align-self: center;
            font-family: sans-serif;
        }
        </style>
    </head>
    <body>
        <div class="home-container">
            <h1 class="home-text">Rails 7 Demo App</h1>
        </div>
        <section class="container-fluid">
            <h3>Categories:</h3>
            <% @categories.each do |category| %>
                <%= category.id %>
                <%= category.name %>
                <hr>
            <% end %>
        </section>
    </body>
</html>